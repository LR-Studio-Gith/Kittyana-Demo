/// @desc The Unstucker
move_outside_all(90, vsp) // All that it is

/*
	Basically it pushes the player 
	until they're not stuck inside the ground anymore

	It also fixes the glitch where the player's hitbox gets stuck 
	inside a slope after flipping around
*/
